# Example .env file for local development
# Copy this file to .env and customize the values as needed
# The .env file is automatically loaded by godotenv before the application starts

# Web Server Configuration
# HOST_PORT=8080
# LOG_FILE=./app.log

# SSL/TLS Configuration (when ssl_disabled = false)
# SSL_CERT_FILE=./tls/cert.pem
# SSL_KEY_FILE=./tls/key.pem

# Secure Cookie Keys (REQUIRED in production)
# Generate new keys by setting regenerate_secure_keys = true in config.toml
# Then copy the generated keys here for production use
# SECURE_COOKIE_SIGNING_KEY=your_64_byte_hex_key_here
# SECURE_COOKIE_ENCRYPTION_KEY=your_32_byte_hex_key_here
